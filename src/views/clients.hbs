{{#if allClients}}

<ul class="clientsContainer">
  {{#each allClients}}
  <li data-id="{{this._id}}" class="clientContainer">
    <div class="left">
      <p>Фамилия: {{this.surname}}</p>
      <p>Имя: {{this.name}}</p>
      <p>Отчество: {{this.patronymic}}</p>
      <div class="dannie">
      <p>Email: {{this.email}}</p>
      <p>Телефон: {{this.phone}}</p>
      <p>Адрес: {{this.address}}</p>
      <p>Добавил клиента: {{this.creator.name}}</p>
      <p>Заказы: </p>
      </div>
      <p class="topcom"><b>Комментарии:</b></p>
      <div class="comments">
        {{#each this.comments}}
        <p>{{this.body}}</p>
        <p>Дата комментария: {{this.date}}</p>
        <p>Автор комментария: {{this.user}}</p>
        <hr>
        {{/each}}
      </div>
    </div>
    <div class="right">
      <form class="leavecomment" id="leavecomment" method="POST">
        <textarea placeholder="Оставьте комментарий о клиенте" name="body" cols="40" rows="3"></textarea>
        <button id="topbtn" type="submit" class="btn-comment btn btn-primary">оставить комментарий</button>
      </form>
      <div class="actionButtons">
        <a href="/clients/{{this._id}}" class="btn btn-primary">редактировать данные клиента</a>
        <a href="/orders/new/{{this._id}}" class="btn btn-primary">создать заказ</a>
        <button data-button='deleteClient' type="submit" class="btn btn-primary">удалить клиента</button>
      </div>
    </div>
  </li>
  <hr>
  {{/each}}
</ul>

{{/if}}


<style>
  body{
    background-color: #efede9;
    background-image: none;
  }
  .dannie{
        margin-left: 67%;
    width: 300px;
    margin-top: -35%;
  }
  .comments{
        height: 167px;
    width: 347px;
    border: 2px solid #88a08a;
    padding: 10px;
    overflow-y: scroll;
    border-radius: 14px;
  }
  .actionButtons {
    display: flex;
    grid-auto-flow: column;
    margin: 15%;
    margin-bottom: -1%;
    margin-top: 33%;
    margin-left: 0%;
    width: 99%;
    justify-content: space-evenly;
    flex-direction: row;
    flex-wrap: nowrap;
    align-content: space-between;
  }
  .btn{
    height: 62px;
    text-transform: uppercase;
    font-size: 14px;
    -webkit-text-security: none;
}
.btn-primary:hover {
    color: #fff;
    background-color: #4c6d4d;
    border-color: #415442;
}
.btn-primary {
    color: #fff;
    background-color:#88a08a;
    border-color: #a9c7aa;
}
.clientContainer{
      border-style: solid;
    border-radius: 14px;
    border-color: #88a08a;
    border-width: thin;
}
.left{
  padding: 29px;
}
.right{
  padding: 29px;
}

#topbtn{
  margin-left: 50%;
}
textarea {
    resize: vertical;
    border-color: #4c6d4d;
    border-radius: 11px;
    background-color: #efede9;
    text-indent: 13px;
}


  </style>
